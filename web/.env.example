# The Google OAuth Web client id. This is the SAME value as the server's
# GOOGLE_CLIENT_ID — the VITE_ prefix only controls whether Vite inlines it
# into the browser bundle; both vars reference the one OAuth client.
VITE_GOOGLE_CLIENT_ID=your-google-web-oauth-client-id.apps.googleusercontent.com
